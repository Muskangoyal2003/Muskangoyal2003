string reverseWord(string str){
    
  //Your code 
  int i =0;
  int j = str.size()-1;
    while(i<=j){
      char temp = str[j];
      str[j]=str[i];
      str[i]=temp;
       i++;
      j--;
  }
  return str;
}
